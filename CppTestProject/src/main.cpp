#include <iostream>

using namespace std;
/*template <typename T>
typedef struct Node
{
	Node(T value = 0) :m_data(value)
	{}

	T m_data;
};

template<typename T>
typedef struct BNode : public Node<T>
{
	BNode(T value = 0) : Node <T>(value)
	{}

	BNode *m_pL;	// Left Child
	BNode *m_pR;	// Right Child
	BNode *m_pP;	// Parent node
};*/


int main()
{

	return 0;
}